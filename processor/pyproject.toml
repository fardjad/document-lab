[project]
name = "document-cropper-processor"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard]>=0.115,<1",
    "pyyaml>=6.0.3",
]

[tool.fastapi]
entrypoint = "main:app"

[dependency-groups]
dev = [
    "httpx2>=2.0.0",
]
